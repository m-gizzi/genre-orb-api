en:
  oauth:
    errors:
      access_denied: "You declined to connect your Spotify account"
      invalid_credentials: "Spotify rejected our app credentials. Please contact support."
      invalid_request: "The authorization request was invalid. Please try again."
      server_error: "Spotify is having issues. Please try again later."
      temporarily_unavailable: "Spotify is temporarily unavailable. Please try again later."
      default: "Authentication failed. Please try again."
  api:
    errors:
      spotify_not_connected: "Spotify not connected"
      spotify_unavailable: "Spotify is temporarily unavailable. Your changes were not saved."
      spotify_rate_limited: "Spotify is rate limiting us. Please try again in a moment."
      unauthenticated: "You need to sign in or sign up before continuing"
      not_found: "Resource not found"
      bad_request: "Invalid request"
    library:
      sync_in_progress: "Sync already in progress"
      no_playlists_selected: "No playlists selected for sync"
    smart_playlists:
      target_required: "A target playlist is required"
      evaluation_timeout: "These rules took too long to evaluate. Try simplifying them."
    artists:
      sync_in_progress: "Artist metadata sync already in progress"
      no_artists_need_sync: "No artists need metadata sync"
  rules:
    locators:
      rule: "%{message} at rule %{path}"
      group: "%{message} at group %{path}"
    errors:
      group_shape: "must have 'match' and 'rules' keys"
      too_deep: "is nested more than %{max} levels deep"
      too_many_nodes: "cannot contain more than %{max} rules"
      rules_not_a_list: "'rules' must be a list"
      empty_group: "must contain at least one rule"
      unknown_match: "'match' must be one of: %{match_types}"
      invalid_negation: "'not' must be true or false"
      condition_shape: "each rule must be an object"
      unknown_field: "has an unknown field: %{field}"
      unknown_operator: "has an unknown operator: %{operator}"
      unsupported_pairing: "does not support the operator %{operator} on the field %{field}"
      unknown_keys: "has unexpected keys: %{keys}"
      missing_value: "each rule must have a value"
      not_a_scalar: "must have a single value"
      not_a_pair: "must have exactly two values when comparing a range"
      entry_shape: "must contain only single values"
      inverted_range: "must not have a lower bound greater than its upper bound"
      incomparable_range: "must have two bounds of the same kind"
      not_a_list: "must have at least one value when matching a list"
      list_too_long: "cannot match more than %{max} values at once"
      relative_shape: "must have a count and a unit"
      relative_count: "must have a whole number count"
      relative_unit: "must use one of these units: %{units}"
      not_text: "must be text"
      blank_text: "must not be blank"
      text_too_long: "must be %{max} characters or fewer"
      not_a_number: "must be a whole number"
      out_of_range: "must be between %{min} and %{max}"
      not_a_boolean: "must be true or false"
      not_a_date: "must be a date in YYYY-MM-DD form"
